{
  "ConnectionStrings": {
    "Default": "Server=localhost;Database=cvagora;User=cvagora_user;Password=${DB_PASSWORD};CharSet=utf8mb4;"
  },
  "AdminCredentials": {
    "Username": "${ADMIN_USER}",
    "Password": "${ADMIN_PASSWORD}"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Error"
    }
  }
}
